name: Workflow Events1

on:
  push:
  pull_request:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: echo "This workflow was triggered by a(n) ${{ github.event_name }} event." 